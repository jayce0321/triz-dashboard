[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn TRIZ_ASIT_대시보드:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
