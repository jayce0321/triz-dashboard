# CLAUDE.md

이 폴더의 작업은 아래 지침을 따른다.

## 언어
- 한국어로 응답한다.
- 진행상황을 실시간으로 계속 보고한다.

## 브랜드
- 포인트 컬러: `#2367d7` (세계일보 시그니처 블루)
- 폰트: 제목은 `세계하나제목고딕`, 본문은 `세계하나본문명조`를 사용한다. 미설치 시 시스템 기본 한글 폰트로 대체한다.

## 파일 인코딩
- CSV·TXT는 UTF-8 with BOM(`utf-8-sig`)으로 저장한다.
- 압축은 Python `zipfile` 모듈만 사용한다.

## 산출물
- 결과 파일명은 한글로 작성한다.
- 현재 작업 디렉토리에 저장한다.
- 작업 완료 즉시 `open` 명령으로 파일을 연다.

## 날짜·시간
- 한국 시각(Asia/Seoul)을 기준으로 한다.
- 날짜는 `YYYY-MM-DD` 형식으로 표기한다.

## 멀티에이전트 협업
- "팀으로", "멀티에이전트로", "에이전트 여러 명", "미니 편집국처럼" 같은 요청이 나오면 **여러 에이전트를 띄워 협업시키는 기능을 사용한다.** Claude Code에는 이 기능이 있다 — "그런 기능은 없다"거나 "한 번에 하나씩 하겠다"고 답하지 않는다.
- 사용하는 도구와 순서:
  1. `TeamCreate` 로 팀을 만든다 (`team_name` 지정 — 팀을 만들면 공유 작업 목록도 같이 생긴다).
  2. `Agent` 로 팀원을 띄운다. `team_name` 과 `name`(예: 취재기자·데스크·팩트체커·교열)을 지정해 팀에 합류시킨다. 각 팀원에게 역할·말투·관점을 구체적으로 부여한다.
  3. `SendMessage` 로 **팀원끼리 직접 메시지를 주고받게 한다.** `to` 에 상대 팀원 이름을 넣는다. 결과만 따로 모으지 말고, 한 팀원이 막히거나 넘길 게 생기면 다른 팀원에게 메시지를 보내 같이 풀게 한다 (예: 취재기자 초안 → 팩트체커에게 검증 요청 → 데스크에게 구조 검토 요청).
  4. `TaskCreate` / `TaskList` 로 할 일을 공유 작업 목록에 쪼개 팀원이 나눠 맡게 한다.
  5. 일이 끝나면 팀원을 정리하고 `TeamDelete` 로 팀을 닫는다.
- 메인은 허브 역할을 한다. 팀원 메시지는 자동으로 전달되며, 막힌 팀원이 있으면 다른 팀원에게 넘기고, 끝나면 각 팀원이 무엇을 했는지와 최종 산출물을 한눈에 정리해 보고한다.
- 한 명짜리 역할·페르소나 에이전트(예: 데스크 관점으로 검토하는 에이전트)는 `Agent` 만으로 만들 수 있다.
- 작업을 시작하기 직전, 진행 상황을 바로 보며 갈지(포그라운드) 백그라운드로 돌릴지 한 번 확인한다.
- 역할이 갈리지 않는 단순한 작업은 팀을 띄우지 않는다.

## 에이전트·스킬 생성 위치
- 에이전트·스킬을 만들 때 위치 지정이 따로 없으면, 전역(`~/.claude/`)이 아니라 **현재 프로젝트 폴더 안의 `.claude/`** 에 생성한다.
- 에이전트는 `.claude/agents/`, 스킬은 `.claude/skills/` 에 둔다.
