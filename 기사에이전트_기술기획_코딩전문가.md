# 세계일보 기사 에이전트 시스템 — 기술 기획안

> 작성: 시니어 풀스택 엔지니어 (AI/LLM 백엔드 설계)
> 작성일: 2026-06-12
> 벤치마크: 서울경제 AI-LINK (WAN-IFRA DMA 2026 글로벌 우승)
> 기반 코드: `aeo/aeo_dashboard.py` (FastAPI + Claude, 880 LOC 검토 완료)

---

## 0. 요약 (Executive Summary)

서울경제 AI-LINK는 **풀 AWS 서버리스(Bedrock + Lambda + Step Functions + DynamoDB)** 로 6명이 1년에 걸쳐 구축했다. 우리는 그 철학("기자의 노하우를 프롬프트에 내장", "환각 방지 3중 구조")은 그대로 차용하되, **인프라는 우리 현실(Python·FastAPI·Railway, 소수 인원)에 맞게 모놀리식 → 점진적 분리** 로 간다.

핵심 판단 3가지:

1. **기존 `aeo/`는 4번째 기능(AEO 변환)의 완성된 구현체다. 버리지 말고 첫 모듈로 흡수한다.**
2. **나머지 3개 기능(제목추천·보도자료기사화·교열)은 신규 모듈로 분리하되, 같은 FastAPI 앱 안에 라우터로 묶는다.** (서버리스 분산은 트래픽이 증명된 뒤로 유보)
3. **모델은 기능별로 Haiku/Sonnet/Opus를 갈라 쓴다.** 비용의 80%는 모델 선택과 프롬프트 캐시에서 결정된다.

---

## 1. 기술 아키텍처

### 1.1 설계 원칙

| 원칙 | AI-LINK(벤치마크) | 우리 선택 | 근거 |
|---|---|---|---|
| 인프라 | AWS 서버리스 풀스택 | FastAPI 모놀리식 + Railway | 6명·1년 vs 소수·단기. 서버리스 오케스트레이션 학습비용이 ROI를 압도 |
| LLM 게이트웨이 | Amazon Bedrock | Anthropic API 직접 호출 | Bedrock 경유 시 한국 리전 지연·프롬프트 캐시 제약. 직접 호출이 단순·저렴 |
| 상태 저장 | DynamoDB | JSON 파일 → SQLite → Postgres(단계적) | 현재 트래픽에 DynamoDB는 과설계. 단 영속화는 1순위 개선 |
| 인증 | Cognito | FastAPI 의존성 + API Key/JWT | 사내 편집국 한정. Cognito 불필요 |
| 캐시 | Lambda 캐시 + Anthropic 프롬프트 캐시 | **Anthropic 프롬프트 캐시 + 인메모리 LRU** | 동일 효과를 더 단순하게 |

### 1.2 3레이어 구조 (AI-LINK 차용)

```
┌─────────────────────────────────────────────────────────────┐
│  [1] EDGE & AUTH 레이어                                       │
│  - 정적 대시보드 (HTML/JS, #2367d7 브랜드)                     │
│  - CORS / 요청 검증 / API Key·JWT 인증 의존성                  │
│  - Rate Limit (slowapi) — 편집국 IP 화이트리스트              │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│  [2] COMPUTE 레이어  (FastAPI / APIRouter 분리)               │
│                                                              │
│   /api/title/*    ──▶ 제목추천 모듈   (Sonnet)               │
│   /api/press/*    ──▶ 보도자료기사화  (Sonnet)               │
│   /api/proof/*    ──▶ 교열 모듈       (Opus, 정확도 우선)    │
│   /api/aeo/*      ──▶ AEO 변환 모듈   (Haiku, 기존 코드)     │
│                                                              │
│   ┌──────────────── 공유 코어 (core/) ──────────────────┐    │
│   │ llm_client : Claude 호출 + 프롬프트 캐시 + 재시도    │    │
│   │ extractor  : URL/PDF/TXT 본문추출 (기존 fetch/extract)│   │
│   │ ko_analyzer: 한국어 엔티티·수치 휴리스틱 (기존)      │    │
│   │ guard      : 환각 방지 3중 검증 (입력·프롬프트·검수)  │    │
│   │ prompts/   : 5계층 프롬프트 템플릿 (버전 관리)        │    │
│   └──────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│  [3] STORAGE & DATA 레이어                                    │
│  - 작업 이력 DB (JSON → SQLite → Postgres)                    │
│  - 프롬프트 버전 저장소 (Git 관리, prompts/*.md)              │
│  - 산출물 캐시 (입력 해시 → 결과, TTL)                        │
│  - 감사 로그 (누가/언제/무엇을 승인·반려)                     │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 디렉토리 구조 (제안)

```
aeo/                          # 기존 폴더를 루트 패키지로 승격
├── main.py                   # FastAPI 앱 + 라우터 등록 (기존 aeo_dashboard.py 분해)
├── core/
│   ├── llm_client.py         # Claude 호출 통합 (캐시·재시도·모델 라우팅)
│   ├── extractor.py          # fetch_article / extract_from_file 이동
│   ├── ko_analyzer.py        # analyze_korean + 정규식 패턴 이동
│   ├── guard.py              # 환각 방지 검증 (사실 보존 스코어 재사용)
│   └── prompts/
│       ├── title.md
│       ├── press.md
│       ├── proofread.md
│       └── aeo.md            # 기존 AEO_SYSTEM 이동
├── routers/
│   ├── title.py
│   ├── press.py
│   ├── proofread.py
│   └── aeo.py                # 기존 /api/convert 등 이동
├── models.py                 # Pydantic 요청/응답 스키마 통합
├── storage.py                # load_db/save_db → 추상화 (JSON↔SQLite 스왑)
└── static/
    └── dashboard.html
```

---

## 2. 기존 `aeo/` 확장 전략

### 2.1 결론: **"신규 모듈로 분리하되 한 앱으로 통합"** (하이브리드)

| 선택지 | 장점 | 단점 | 판정 |
|---|---|---|---|
| A. 기존 파일에 함수 추가 | 빠름 | 880줄 단일 파일이 1,500줄+로 비대화, 충돌·테스트 난이도↑ | ✗ |
| B. 기능별 완전 분리 마이크로서비스 | 독립 배포 | 본문추출·한국어분석·LLM클라이언트 **중복 구현 불가피**, 운영부담↑ | ✗ |
| **C. 라우터 분리 + 코어 공유 (모듈러 모놀리스)** | 코드 재사용 최대, 단일 배포, 점진적 분해 가능 | 초기 리팩토링 1회 필요 | **✓ 채택** |

### 2.2 근거

기존 코드를 분석하면 **3개 신규 기능이 즉시 재사용할 자산**이 이미 검증된 상태로 존재한다:

- `fetch_article()` (107–149행): URL 크롤링 + 세계일보 DOM 셀렉터 → **보도자료 URL 입력**에 그대로 사용
- `extract_from_file()` (152–215행): PDF/HTML/TXT 추출 → **모든 기능의 파일 업로드** 공통 입력
- `analyze_korean()` + 정규식 패턴 (57–232행): 수치·인용·엔티티 추출 → **교열의 사실관계 검증, 제목추천의 핵심 키워드 추출**에 재사용
- `score_preservation()` (320–325행): 원문 대비 보존율 → **환각 방지 검증(guard)** 의 핵심 엔진으로 승격
- `_parse_aeo_json()` (273–289행): 4단계 방어 JSON 파싱 → 모든 구조화 출력 기능 공용

→ B(마이크로서비스)를 택하면 위 5개를 매 기능마다 복붙해야 한다. 이는 명백한 안티패턴.
→ A(단일 파일)는 협업·테스트가 막힌다.
→ **C가 유일하게 합리적.** 1회 리팩토링으로 `core/`에 추출하고, 기능은 얇은 라우터로 붙인다.

### 2.3 마이그레이션 순서 (무중단)

1. `aeo_dashboard.py` 동작 유지한 채 `core/` 패키지 신설, 함수를 **이동이 아니라 import 재노출**로 먼저 분리
2. 기존 엔드포인트가 `core`를 바라보도록 교체 → 회귀 테스트
3. 신규 라우터(title/press/proof) 추가
4. 안정화 후 `aeo_dashboard.py`를 `main.py` + `routers/aeo.py`로 최종 분해

---

## 3. 4개 기능별 API 설계

공통 규약:
- Base path: `/api`
- 인증: `Authorization: Bearer <편집국 토큰>`
- 긴 작업(보도자료·AEO)은 **SSE 스트리밍**(기존 `_sse` 패턴 재사용), 짧은 작업(제목)은 동기 JSON
- 모든 응답에 `request_id`, `model`, `usage`(토큰), `latency_ms` 포함 → 비용·품질 추적

### 3.1 기사 제목 추천 — `POST /api/title/suggest`

```jsonc
// 요청
{
  "body": "기사 초안 전문…",           // body 또는 url 또는 file 중 하나
  "url": null,
  "count": 8,                          // 7~10, 기본 8
  "tone": ["정통", "후킹"],            // 스타일 힌트(선택)
  "constraints": { "max_chars": 45 }   // 제목 글자수 상한(선택)
}

// 응답 (동기)
{
  "request_id": "uuid",
  "model": "claude-sonnet-4-6",
  "titles": [
    { "text": "기준금리 또 동결…한은 '물가 불씨' 경계", "chars": 22,
      "type": "정통형", "key_entities": ["한국은행", "기준금리"],
      "rationale": "핵심 수치+행위주체 선두 배치" },
    { "text": "동결의 속내…한은이 끝내 못 내린 이유", "chars": 20,
      "type": "후킹형", "key_entities": ["한국은행"], "rationale": "..." }
  ],
  "usage": { "input_tokens": 1820, "output_tokens": 540, "cache_read": 1500 },
  "latency_ms": 2100
}
```

### 3.2 보도자료 기사화 — `POST /api/press/rewrite` (SSE)

```jsonc
// 요청
{
  "source_text": "보도자료 원문…",     // text 또는 url
  "url": null,
  "article_type": "스트레이트",        // 스트레이트 | 해설 | 단신
  "target_length": 800,               // 어절 수 목표
  "include_lead": true                // 리드 문단 자동 생성
}

// SSE 이벤트 흐름 (기존 /api/convert와 동일 패턴)
// event: progress  data: 보도자료 추출 중…
// event: progress  data: 핵심 사실 12건 / 인용 3건 감지. 기사화 중…
// event: done      data: { 아래 record }
{
  "request_id": "uuid",
  "model": "claude-sonnet-4-6",
  "headline": "...",
  "lead": "리드 문단",
  "body": "기사 본문(세계일보 문체)",
  "extracted_facts": { "numbers": [...], "quotes": [...], "entities": [...] },
  "preservation_score": 98.2,         // guard: 원문 사실 보존율
  "warnings": ["info:인용 3건 모두 보존"],
  "status": "pending"                 // 인간 검수 대기
}
```

### 3.3 교열 — `POST /api/proof/check` (SSE)

```jsonc
// 요청
{
  "body": "기사 초안 전문…",
  "level": "표준",                     // 가벼움 | 표준 | 엄격
  "check": ["문법", "표현", "사실관계", "표기법"]
}

// 응답
{
  "request_id": "uuid",
  "model": "claude-opus-4-8",         // 정확도 최우선 → Opus
  "corrected_body": "교정된 전문(반영본)",
  "diffs": [
    { "span": [120, 128], "before": "되어집니다", "after": "됩니다",
      "category": "문법", "severity": "high",
      "reason": "이중 피동 표현" },
    { "span": [300, 312], "before": "2025년 12월", "after": "2024년 12월",
      "category": "사실관계", "severity": "critical",
      "reason": "원문 타 문단과 연도 불일치 — 확인 필요",
      "needs_human": true }            // 사실관계는 단정하지 않고 플래그만
  ],
  "summary": { "문법": 4, "표현": 7, "사실관계": 1, "표기법": 2 },
  "status": "pending"
}
```

> 사실관계 교정은 **자동 수정하지 않고 `needs_human: true`로 플래그만** 한다. 환각 방지의 핵심.

### 3.4 AEO 변환 — `POST /api/aeo/convert` (기존, 경로만 정리)

기존 `/api/convert`, `/api/batch`, `/api/upload`, `/api/articles/*`, `/api/export`를 `routers/aeo.py`로 이동. **스키마·동작 100% 유지** (회귀 위험 0). 신규 기능과 응답 포맷(`request_id`/`usage`/`latency_ms`)만 통일.

---

## 4. 프롬프트 엔지니어링 전략

### 4.1 시스템 프롬프트 5계층

AI-LINK의 "기자 25년 노하우 내장" 철학을 **계층화된 프롬프트 템플릿**으로 구조화. 각 계층은 `core/prompts/*.md`에 분리 저장 → 버전 관리·A/B 테스트 가능.

```
┌─ L1. 정체성 (Identity) ──────────────────────────────────
│   "당신은 세계일보 편집국 25년차 데스크다." 역할·권위 부여.
│   → 모든 기능 공통. 프롬프트 캐시 대상(고정).
├─ L2. 매체 스타일 (House Style) ──────────────────────────
│   세계일보 문체·어휘·금칙어·제목 관행. 실제 기사 few-shot 5~8개.
│   → 기능별로 다름(제목용/기사용/교열용). 캐시 대상.
├─ L3. 작업 규칙 (Task Rules) ─────────────────────────────
│   기능별 구체 지침. 예: 제목=7~10개·글자수·유형 다양성 /
│   교열=수정근거 필수·사실관계 단정 금지. (기존 AEO_SYSTEM이 이 계층)
├─ L4. 출력 계약 (Output Contract) ────────────────────────
│   엄격한 JSON 스키마 + "코드블록 금지" + 예시. _parse_aeo_json 방어.
├─ L5. 가드레일 (Guardrails) ──────────────────────────────
│   "원문에 없는 정보 생성 금지. 불확실하면 needs_human 플래그.
│    수치·인명·날짜·인용은 한 자도 변경 금지." → 환각 방지 핵심.
└──────────────────────────────────────────────────────────
```

조립: `system = L1 + L2(기능) + L3(기능) + L4(기능) + L5`. L1·L2는 정적이라 **프롬프트 캐시 break point**를 L2와 L3 사이에 둔다 → L1+L2 캐시 적중.

### 4.2 환각 방지 3중 구조 (AI-LINK 차용 + 우리 강점 결합)

| 단계 | AI-LINK | 우리 구현 |
|---|---|---|
| ① 입력 제한 | 승인 기사만 입력 | `extractor`로 출처 명확한 본문만 / 보도자료는 원문 첨부 강제 |
| ② 프롬프트 제약 | "원문 내 정보만" | L5 가드레일 + 기능별 "추가 수치 삽입 금지" 명시 |
| ③ 인간 검수 | 에디터 최종 검수 | `status: pending` → 대시보드 승인/반려 (기존 워크플로) |
| **④ 자동 검증(우리 추가)** | — | **`guard.py`가 출력의 수치·인용·엔티티를 원문과 대조해 보존율 계산.** 임계치 미만이면 자동 반려·재시도 |

→ 우리는 기존 `score_preservation`/`score_tone`을 **사후 검증 게이트**로 승격해 4중 구조로 만든다. 이게 차별점.

---

## 5. Claude 모델 선택 (기능별 라우팅)

> 모델 ID 기준 (2026-01): Opus 4.8 `claude-opus-4-8`, Sonnet 4.6 `claude-sonnet-4-6`, Haiku 4.5 `claude-haiku-4-5-20251001`.

| 기능 | 모델 | 근거 |
|---|---|---|
| **AEO 변환** | **Haiku 4.5** | 구조 변환 위주, 창의성 불필요. 기존 검증됨. 최저가 |
| **제목 추천** | **Sonnet 4.6** | 한국어 감수성·후킹·문학적 메타포(AI-LINK가 Claude 택한 이유). Opus는 과함 |
| **보도자료 기사화** | **Sonnet 4.6** | 문체 재현+사실 보존 균형. 대량 처리 → Opus는 비용·지연 부담 |
| **교열** | **Opus 4.8** | 사실관계·미묘한 문법 오류 탐지에 최고 정확도 필요. 오탐 비용이 모델 비용보다 큼 |

**동적 폴백**: `llm_client`에서 `model_for(task)` 헬퍼로 추상화. 환경변수로 오버라이드 가능(`TITLE_MODEL=...`) → 비용 급등 시 Sonnet→Haiku 즉시 강등. 교열만 강등 금지(정확도 SLA).

---

## 6. 비용 최적화

AI-LINK는 5주간 $1,389 → $262 (81%↓). 우리도 **2가지 레버**로 동일 효과를 노린다.

### 6.1 Anthropic 프롬프트 캐시 (1차 레버, ~70% 절감)

- L1(정체성)+L2(매체 스타일+few-shot)는 요청마다 동일 → `cache_control: ephemeral` 적용
- few-shot 예시가 클수록(수천 토큰) 캐시 효과 극대화. 캐시 읽기는 입력 단가의 10% 수준
- 응답에 `cache_read` 토큰을 노출(§3 스키마)해 적중률 모니터링

### 6.2 결과 캐시 + 토큰 관리 (2차 레버)

- **입력 해시 캐시**: `sha256(정규화된 입력 + task + model + 프롬프트버전)` → 결과 저장(TTL 24h). 동일 보도자료 재처리·데모 반복 시 LLM 호출 0
- **본문 트렁케이션**: 기존 `body[:4000]` 패턴 유지·기능별 조정(제목은 2000자면 충분 → 토큰 절감)
- **배치 처리**: 야간 대량 AEO 변환은 Message Batches API로(50% 할인)
- **max_tokens 타이트하게**: 제목=800, 교열 diff=2048 등 기능별 상한

### 6.3 모니터링

`usage` 로그를 storage에 적재 → 일/기능/모델별 비용 대시보드. AI-LINK처럼 "어디서 새는지" 먼저 측정해야 줄인다.

---

## 7. 개발 리스크 및 대응

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | **JSON 출력 깨짐** (LLM이 코드블록·잡설 첨가) | 파싱 실패 | 기존 `_parse_aeo_json` 4단계 방어 전 기능 적용 + L4 출력계약 강화 + 재시도(기존 retry 패턴) |
| R2 | **환각**(원문에 없는 수치·인용 생성) | 오보 — 최악 | §4.2 4중 구조. `guard` 보존율 게이트가 임계치 미달 시 자동 반려 |
| R3 | **교열 사실관계 오판** | 멀쩡한 사실을 "틀렸다"고 수정 | 사실관계는 자동수정 금지, `needs_human` 플래그만 (§3.3) |
| R4 | **세계일보 DOM 변경** | URL 크롤링 실패 | 기존 다단계 셀렉터 폴백 유지 + `<p>` 최후 폴백 + 추출 실패 시 명시적 에러 |
| R5 | **비용 폭증** (트래픽·긴 입력) | 예산 초과 | §6 캐시 + max_tokens 상한 + 모델 환경변수 강등 스위치 + 비용 알람 |
| R6 | **API 키 노출** | 보안 | 기존 `~/.anthropic/triz.env` 분리 유지. Railway는 환경변수 시크릿. 키 커밋 금지 |
| R7 | **CORS `allow_origins=["*"]`** | 운영 보안 구멍 | 배포 시 편집국 도메인 화이트리스트로 교체 + 인증 의존성 추가 |
| R8 | **데이터 영속성** (JSON 파일, 동시쓰기) | 데이터 유실·레이스 | `storage.py` 추상화 → SQLite(WAL) → Postgres 단계 이전. 현재 JSON은 단일 인스턴스 한정 |
| R9 | **단일 인스턴스 SPOF** | 가용성 | 초기엔 허용. 트래픽 증명 후 stateless화→다중 인스턴스(이때 DB 분리 선행 필수) |
| R10 | **프롬프트 회귀** (개선이 품질 저하) | 은밀한 품질 하락 | 프롬프트 Git 버전관리 + 골든셋 기사로 A/B 평가(기존 `evaluate` 스코어 재사용) |

---

## 8. 단계별 실행 로드맵

| 단계 | 범위 | 산출물 |
|---|---|---|
| **P0** (기반) | `core/` 추출 리팩토링, `llm_client`(캐시·라우팅), `storage` 추상화 | 기존 AEO 무중단 동작 |
| **P1** (제목) | `/api/title/suggest` + 프롬프트 5계층 + 대시보드 탭 | 가장 단순·임팩트 큰 기능 먼저 |
| **P2** (보도자료) | `/api/press/rewrite` SSE + guard 보존율 게이트 | 핵심 가치 기능 |
| **P3** (교열) | `/api/proof/check` + diff/needs_human + Opus | 정확도 검증 집중 |
| **P4** (운영화) | 인증·CORS·비용 대시보드·골든셋 A/B | 프로덕션 준비 |

> 4개 기능을 한 번에 짓지 않는다. P1(제목)으로 5계층 프롬프트·캐시·검증 파이프라인을 먼저 검증한 뒤, 같은 틀을 P2·P3에 복제하는 것이 가장 빠르고 안전하다.

---

*문체·브랜드: 포인트 컬러 #2367d7 / 제목 세계하나제목고딕 / 본문 세계하나본문명조. 산출물 파일명 한글, KST 기준.*
